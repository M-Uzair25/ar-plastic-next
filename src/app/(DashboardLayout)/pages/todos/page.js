'use client'
import { Row, Col, Table, Card, CardTitle, CardBody, Button, FormGroup, Label, Input } from "reactstrap";

const todos = () => {
    return (
        <>This will be our todos list reminder</>
    );
};

export default todos;
